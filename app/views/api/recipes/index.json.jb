@recipes.map do |recipe| 
  {
  recipe_id: recipe.id,
  recipe: recipe.name,
  alcoholic?: recipe.alcoholic,
  instructions: recipe.instructions,
  ingredients: recipe.ingredients,
  measurements: recipe.ingredient_recipes.each do |measurement|
    measurement[:measurement]
  end,
  }
end